---
name: using-<product-or-subsystem>
description: |
  Trigger this skill when the user is trying to <concrete task>, deciding
  between <product> and <common alternatives>, or working with <specific
  product concepts>. Keep this narrow enough to avoid false positives.
---

# Using <Product Or Subsystem>

## When to reach for <product or subsystem>

Describe concrete trigger signals. Name the jobs where this product or subsystem is the right tool and the competing choices an agent might otherwise reach for.

## What <product or subsystem> is

Give the durable mental model in one compact section. Avoid marketing language. Explain the product in terms that help an agent make implementation decisions.

## Quick start

Provide the smallest useful example. Prefer copy-pasteable commands or code. Do not use ellipses in runnable snippets.

## When NOT to use <product or subsystem>

List the cases where this product or subsystem is the wrong fit, too heavy, too early, or likely to mislead an implementation.

## Going deeper

- `references/<topic>.md`: Read when <specific condition>.
- `examples/<example>`: Use when <specific implementation need>.
